name: 'render content.md to pages'
on:
  push:
    branches:
      main
  
jobs:
  call-workflow:
    uses: eliamarcantognini/template-readable-text/.github/workflows/render_content_to_pages.yml@main
    secrets: inherit